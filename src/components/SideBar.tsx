import React from "react";

const SideBar = () => {
	return (
		<aside className='fixed top-0 left-0 z-40 w-64 h-screen transition-transform -translate-x-full sm:translate-x-0'></aside>
	);
};

export default SideBar;
